# Pinned so CI always tests against a known Home Assistant release.
# pytest-homeassistant-custom-component pins homeassistant, pytest, pytest-cov and
# friends itself -- do not pin those separately or the resolver will fight itself.
# 0.13.355 -> homeassistant 2026.8.1 (the minimum this integration supports).
pytest-homeassistant-custom-component==0.13.355

# Runtime dependency, kept in lockstep with manifest.json.
pynetro==0.1.5

# Linting/formatting, kept in lockstep with .pre-commit-config.yaml.
ruff==0.16.2
